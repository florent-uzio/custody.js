# CLAUDE.md

Conventions for AI-assisted development in this repository.

## Types must come from the OpenAPI-generated file

All SDK types must be derived from the files generated by
`npm run generate:custody-types` from the OpenAPI specs bundled under
`openapi/`, organized by channel (`openapi/official/`, `openapi/devbox/`) and by
surface (channel root = public, `<channel>/internal/` = internal):

- `src/models/custody-types.ts` — the **public** API (`/v1/…`, `Core_*`), used
  by every namespace unless it targets an internal endpoint;
- `src/models/custody-internal-types.ts` — the **internal** API
  (`/internal/v1/…` and `/api/notifications/…`, `Internal_*` /
  `Notification_*`). Import it explicitly; it is deliberately not
  re-exported from `src/models/index.ts`, and its `operations` map is separate
  because internal and public specs share some `operationId`s (`getUsers`,
  `getAllEvents`).

See [ADR-0005](docs/adr/0005-official-vs-devbox-channels.md),
[ADR-0007](docs/adr/0007-public-vs-internal-surfaces.md) and `CONTEXT.md`.

Reference the generated types via the two top-level exports:

- `operations["<operationId>"]["parameters"]["path" | "query"]` — request path
  and query param shapes
- `operations["<operationId>"]["requestBody"]["content"]["application/json"]` —
  request body shapes
- `operations["<operationId>"]["responses"][<status>]["content"]["application/json"]` —
  response shapes (when not already a named component)
- `components["schemas"]["<SchemaName>"]` — reusable response and body schemas

Example (from `src/namespaces/events.types.ts`):

```ts
import type { components, operations } from "../models/custody-types.js"

export type GetEventsPathParams = operations["getEvents"]["parameters"]["path"]
export type GetEventsQueryParams = operations["getEvents"]["parameters"]["query"]
export type Core_EventsCollection = components["schemas"]["Core_EventsCollection"]
```

Type aliases live beside the namespace that uses them (`src/namespaces/<name>.types.ts`
next to `src/namespaces/<name>.ts`), not under `src/services/`. `src/services/` is
reserved for the modules with real implementation logic (`apis`, `auth`, `channels`,
`keypairs`, `xrpl`) — everything else is a namespace, and its types co-locate with it.

## Do not hand-author types that duplicate or conflict with the generated file

Hand-written interfaces or type aliases that mirror OpenAPI schemas are not
allowed — they drift from the API contract and make the SDK lie about what the
server actually accepts and returns.

Never edit the _contents_ of the OpenAPI specs under `openapi/` (in any channel
or surface folder) — they are canonical artifacts from the Ripple Custody
server. Relocating a spec into a channel or `internal/` subfolder is fine;
rewriting its JSON is not.

The only types that may be hand-authored are SDK-internal types that have no
API counterpart (for example, polling option bags like `WaitForExecutionOptions`,
or unions composed from existing generated types like `EDS_ChannelCreate`).

## Agent skills

### Issue tracker

Issues and PRDs are tracked as GitHub issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default triage vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
